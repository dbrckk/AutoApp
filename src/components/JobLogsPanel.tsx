import type { AutoAppState } from "../hooks/useAutoApp";
export function JobLogsPanel({ app }: { app: AutoAppState }) {
  const logs = app.jobLogs || [];
  return <section className="glass-panel rounded-[2rem] p-5"><div className="mb-5 flex items-start justify-between gap-3"><div><h2 className="text-2xl font-black text-white">Logs</h2><p className="mt-1 text-sm text-slate-500">Build, repair, improve and export events.</p></div><button onClick={() => app.handleLoadJobLogs(app.activeJobId)} disabled={app.busy || !app.activeJobId} className="min-h-11 rounded-2xl border border-white/10 bg-white/[0.04] px-4 text-xs font-black text-white disabled:opacity-50">Refresh</button></div><div className="grid gap-2">{logs.length ? logs.slice(0, 80).map((log, index) => <article key={`${log}-${index}`} className="soft-card rounded-2xl p-4"><p className="text-sm leading-6 text-slate-300">{log}</p></article>) : <div className="rounded-3xl border border-dashed border-white/10 p-8 text-center"><p className="font-black text-white">No logs loaded</p><p className="mt-2 text-sm text-slate-500">Open a project or run an autonomous cycle.</p></div>}</div></section>;
}
