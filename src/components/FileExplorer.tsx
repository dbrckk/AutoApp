import { useMemo, useState } from "react";
import type { AutoAppState } from "../hooks/useAutoApp";
import { CodeEditor } from "./CodeEditor";
export function FileExplorer({ app }: { app: AutoAppState }) {const [query,setQuery]=useState("");const filteredFiles=useMemo(()=>{const q=query.trim().toLowerCase();if(!q)return app.files;return app.files.filter((file)=>`${file.path}\n${file.content||""}`.toLowerCase().includes(q))},[app.files,query]);function updateCurrentFile(content:string){if(!app.selectedFile)return;app.setFiles((previous)=>previous.map((file)=>file.path===app.selectedFile?.path?{...file,content}:file))}return <section className="glass-panel overflow-hidden rounded-[2rem]"><div className="border-b border-white/10 p-5"><div className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div><h2 className="text-2xl font-black tracking-tight text-white">Files</h2><p className="mt-1 text-sm text-slate-500">{app.projectStats.files} files · {app.projectStats.lines.toLocaleString()} lines · {app.projectStats.chars.toLocaleString()} chars</p></div><div className="flex flex-wrap gap-2"><button onClick={app.handleCreateFile} className="min-h-10 rounded-2xl border border-white/10 bg-white/[0.04] px-4 text-xs font-black text-white">New</button><button onClick={app.handleRenameSelectedFile} disabled={!app.selectedFile} className="min-h-10 rounded-2xl border border-white/10 bg-white/[0.04] px-4 text-xs font-black text-white disabled:opacity-40">Rename</button><button onClick={app.handleDeleteSelectedFile} disabled={!app.selectedFile} className="min-h-10 rounded-2xl border border-red-400/25 bg-red-500/10 px-4 text-xs font-black text-red-200 disabled:opacity-40">Delete</button></div></div><div className="mt-4 flex gap-2"><input value={query} onChange={(event)=>setQuery(event.target.value)} placeholder="Search files..." className="input-premium min-h-12 min-w-0 flex-1 rounded-2xl px-4 text-sm"/><button onClick={()=>setQuery("")} className="min-h-12 rounded-2xl border border-white/10 bg-white/[0.04] px-4 text-sm font-black text-white">Clear</button></div></div><div className="grid min-h-[620px] lg:grid-cols-[300px_1fr]"><aside className="max-h-[360px] overflow-auto border-b border-white/10 bg-black/20 p-2 lg:max-h-[760px] lg:border-b-0 lg:border-r">{filteredFiles.length?filteredFiles.map((file)=>{const active=app.selectedFile?.path===file.path;return <button key={file.path} onClick={()=>app.setSelectedPath(file.path)} className={`mb-1 block w-full rounded-2xl px-3 py-3 text-left text-sm transition ${active?"bg-violet-600 text-white shadow-[0_0_30px_rgba(124,92,255,0.22)]":"text-slate-400 hover:bg-white/[0.06] hover:text-white"}`}><span className="block truncate font-bold">{file.path}</span><span className="mt-1 block text-[10px] opacity-60">{String(file.content||"").split("\n").length} lines</span></button>}):<div className="rounded-2xl border border-dashed border-white/10 p-5 text-sm text-slate-500">No files found.</div>}</aside><main className="min-h-[560px] bg-black/25"><CodeEditor file={app.selectedFile} onChange={updateCurrentFile}/></main></div></section>}
