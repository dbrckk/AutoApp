import type { AutoAppState } from "../hooks/useAutoApp";
export function GitHubHistoryPanel({ app }: { app: AutoAppState }) {
  const commits = app.githubHistory || [];
  return <section className="rounded-3xl border border-white/10 bg-black/20 p-4"><div className="mb-4 flex items-start justify-between gap-3"><div><h3 className="text-sm font-black text-white">GitHub History</h3><p className="mt-1 text-xs text-slate-500">Recent commits from configured repo.</p></div><button onClick={app.handleLoadGitHubHistory} disabled={app.busy || !app.githubRepo} className="min-h-10 rounded-2xl border border-white/10 bg-white/[0.04] px-4 text-xs font-black text-white disabled:opacity-50">Load</button></div><div className="grid gap-2">{commits.length ? commits.slice(0, 10).map((commit: any) => <article key={commit.sha || commit.html_url || commit.url} className="soft-card rounded-2xl p-3"><p className="line-clamp-2 text-sm font-bold text-white">{commit.message || commit.commit?.message || "Commit"}</p><p className="mt-1 text-xs text-slate-500">{(commit.sha || "").slice(0, 7)} - {commit.date || commit.commit?.author?.date || commit.commit?.committer?.date || "unknown date"}</p></article>) : <p className="rounded-2xl border border-dashed border-white/10 p-4 text-sm text-slate-500">No GitHub history loaded.</p>}</div></section>;
}
